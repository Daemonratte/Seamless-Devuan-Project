#!/bin/sh
set -e
# Automatically added by dh_installinit/13.11.8
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/lbcd" ] ; then
	invoke-rc.d lbcd stop || exit 1
fi
# End automatically added section
