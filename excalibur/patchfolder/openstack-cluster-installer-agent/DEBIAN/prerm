#!/bin/sh
set -e
# Automatically added by dh_installinit/13.24.2
if [ -z "$DPKG_ROOT" ] && [ "$1" = remove ] && [ -x "/etc/init.d/openstack-cluster-installer-agent" ] ; then
	invoke-rc.d openstack-cluster-installer-agent stop || exit 1
fi
# End automatically added section
# Automatically added by dh_systemd_start/13.24.2
if [ -z "$DPKG_ROOT" ] && [ "$1" = remove ] && [ -d /run/systemd/system ] ; then
	deb-systemd-invoke stop 'oci-live-host-keys.service' 'oci-live-report-status.service' >/dev/null || true
fi
# End automatically added section
