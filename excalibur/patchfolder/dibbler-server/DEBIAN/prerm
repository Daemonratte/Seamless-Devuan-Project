#!/bin/sh
set -e
# Automatically added by dh_installinit/13.20
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/dibbler-server" ] ; then
	invoke-rc.d dibbler-server stop || exit 1
fi
# End automatically added section
