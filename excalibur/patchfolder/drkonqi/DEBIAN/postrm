#!/bin/sh
set -e

# Automatically added by dh_installsystemduser/13.24.2
if [ "$1" = "purge" ]; then
	if [ -z "$DPKG_ROOT" ] && [ -x "/usr/bin/deb-systemd-helper" ] ; then
		deb-systemd-helper --user purge 'drkonqi-coredump-cleanup.service' 'drkonqi-coredump-cleanup.timer' 'drkonqi-coredump-launcher.socket' 'drkonqi-coredump-pickup.service' 'drkonqi-sentry-postman.path' 'drkonqi-sentry-postman.timer' >/dev/null || true
	fi
fi
# End automatically added section


# Stolen from an example section generated by dh_installsystemd/13.11.4
if [ "$1" = "purge" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper purge 'drkonqi-coredump-processor@.service' >/dev/null || true
	fi
fi
