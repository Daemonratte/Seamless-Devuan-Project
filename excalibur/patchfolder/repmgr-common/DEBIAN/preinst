#!/bin/sh
set -e
# Automatically added by dh_installinit/13.23
if [ "$1" = "install" ] && [ -n "$2" ] && [ -e "/etc/init.d/repmgrd" ] ; then
	chmod +x "/etc/init.d/repmgrd" >/dev/null || true
fi
# End automatically added section
