#!/bin/sh
set -e
# Automatically added by dh_installinit/13.14.1
if [ "$1" = "remove" ] && [ -x "/etc/init.d/istgt" ] ; then
	chmod -x "/etc/init.d/istgt" >/dev/null || true
fi
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = "purge" ] ; then
	update-rc.d istgt remove >/dev/null
fi
# End automatically added section
