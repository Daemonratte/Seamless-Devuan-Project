#!/bin/sh
set -e
# Automatically added by dh_installinit/13.22
if [ "$1" = "install" ] && [ -n "$2" ] && [ -e "/etc/init.d/whereami" ] ; then
	chmod +x "/etc/init.d/whereami" >/dev/null || true
fi
# End automatically added section
