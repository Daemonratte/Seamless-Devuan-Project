#!/bin/sh
set -e
# Automatically added by dh_installinit/13.11.9
if [ "$1" = "install" ] && [ -n "$2" ] && [ -e "/etc/init.d/autodir" ] ; then
	chmod +x "/etc/init.d/autodir" >/dev/null || true
fi
# End automatically added section
