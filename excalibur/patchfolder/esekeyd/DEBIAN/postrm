#!/bin/sh
set -e
# Automatically added by dh_installinit/13.16
if [ "$1" = "remove" ] && [ -x "/etc/init.d/esekeyd" ] ; then
	chmod -x "/etc/init.d/esekeyd" >/dev/null || true
fi
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = "purge" ] ; then
	update-rc.d esekeyd remove >/dev/null
fi
# End automatically added section
