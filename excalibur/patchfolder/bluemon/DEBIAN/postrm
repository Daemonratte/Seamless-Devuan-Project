#!/bin/sh
set -e
# Automatically added by dh_installinit/13.15.3
if [ "$1" = "remove" ] && [ -x "/etc/init.d/bluemon" ] ; then
	chmod -x "/etc/init.d/bluemon" >/dev/null || true
fi
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = "purge" ] ; then
	update-rc.d bluemon remove >/dev/null
fi
# End automatically added section
