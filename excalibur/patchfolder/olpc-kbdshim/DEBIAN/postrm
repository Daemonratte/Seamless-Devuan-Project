#!/bin/sh
set -e
# Automatically added by dh_installinit/13.15.3
if [ "$1" = "remove" ] && [ -x "/etc/init.d/olpc-kbdshim" ] ; then
	chmod -x "/etc/init.d/olpc-kbdshim" >/dev/null || true
fi
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = "purge" ] ; then
	update-rc.d olpc-kbdshim remove >/dev/null
fi
# End automatically added section
