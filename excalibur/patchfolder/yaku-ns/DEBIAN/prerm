#!/bin/sh
set -e
# Automatically added by dh_installinit/13.16
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/yaku-ns" ] ; then
	invoke-rc.d --skip-systemd-native yaku-ns stop || exit 1
fi
# End automatically added section
