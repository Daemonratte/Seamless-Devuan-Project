#!/bin/sh
set -e
# Automatically added by dh_installinit/13.23
if [ "$1" = "install" ] && [ -n "$2" ] && [ -e "/etc/init.d/autofs" ] ; then
	chmod +x "/etc/init.d/autofs" >/dev/null || true
fi
# End automatically added section
