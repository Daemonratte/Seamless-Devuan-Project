#!/bin/sh
set -e
# Automatically added by dh_installinit/13.20
if [ "$1" = "install" ] && [ -n "$2" ] && [ -e "/etc/init.d/isnsdd" ] ; then
	chmod +x "/etc/init.d/isnsdd" >/dev/null || true
fi
# End automatically added section
