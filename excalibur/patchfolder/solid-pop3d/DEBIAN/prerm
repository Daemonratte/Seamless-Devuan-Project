#!/bin/sh
# pre-removal script for Solid POP3

set -e

# Automatically added by dh_installinit/13.3.1
if [ -x "/etc/init.d/solid-pop3d" ] && [ "$1" = remove ]; then
	invoke-rc.d --skip-systemd-native solid-pop3d stop || exit 1
fi
# End automatically added section


case "X$1" in 
	Xupgrade|Xremove)
		update-inetd --disable pop3
		;;
	*)
		;;
esac
