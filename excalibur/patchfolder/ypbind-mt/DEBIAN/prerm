#!/bin/sh
set -e
# Automatically added by dh_installinit/13.15.2
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/ypbind" ] ; then
	invoke-rc.d --skip-systemd-native ypbind stop || exit 1
fi
# End automatically added section
