[Unit]
Description=ICAP server

[Service]
EnvironmentFile=-/etc/default/c-icap
Type=forking
ExecStartPre=/bin/mkdir -p /run/c-icap
ExecStartPre=/bin/chown c-icap:c-icap /run/c-icap
ExecStart=/usr/bin/c-icap
User=root
Group=root

[Install]
WantedBy=multi-user.target
