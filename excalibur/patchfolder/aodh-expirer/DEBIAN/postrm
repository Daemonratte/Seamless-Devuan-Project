#!/bin/sh
set -e
# Automatically added by dh_installinit/13.24.2
if [ "$1" = "remove" ] && [ -x "/etc/init.d/aodh-expirer" ] ; then
	chmod -x "/etc/init.d/aodh-expirer" >/dev/null || true
fi
if [ -z "$DPKG_ROOT" ] && [ "$1" = "purge" ] ; then
	update-rc.d aodh-expirer remove >/dev/null
fi
# End automatically added section
