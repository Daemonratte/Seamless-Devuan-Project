#!/bin/sh
set -e
# Automatically added by dh_installinit/13.5.1
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/ifetch-tools" ] ; then
	invoke-rc.d --skip-systemd-native ifetch-tools stop || exit 1
fi
# End automatically added section
