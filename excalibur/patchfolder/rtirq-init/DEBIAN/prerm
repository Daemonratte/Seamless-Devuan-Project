#!/bin/sh
set -e
# Automatically added by dh_installinit/13.11.6
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/rtirq" ] ; then
	invoke-rc.d --skip-systemd-native rtirq stop || exit 1
fi
# End automatically added section
