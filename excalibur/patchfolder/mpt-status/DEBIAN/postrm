#!/bin/sh
set -e
# Automatically added by dh_installinit/13.15.3
if [ "$1" = "remove" ] && [ -x "/etc/init.d/mpt-statusd" ] ; then
	chmod -x "/etc/init.d/mpt-statusd" >/dev/null || true
fi
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = "purge" ] ; then
	update-rc.d mpt-statusd remove >/dev/null
fi
# End automatically added section
