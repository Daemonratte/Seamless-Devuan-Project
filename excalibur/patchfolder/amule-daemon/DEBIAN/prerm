#!/bin/sh
set -e
# Automatically added by dh_installinit/13.24.1
if [ -z "$DPKG_ROOT" ] && [ "$1" = remove ] && [ -x "/etc/init.d/amule-daemon" ] ; then
	invoke-rc.d --skip-systemd-native amule-daemon stop || exit 1
fi
# End automatically added section
