#!/bin/sh
set -e
# Automatically added by dh_installinit/13.20
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/dibbler-client" ] ; then
	invoke-rc.d dibbler-client stop || exit 1
fi
# End automatically added section
