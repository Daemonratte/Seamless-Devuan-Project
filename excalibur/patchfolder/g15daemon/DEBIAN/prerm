#!/bin/sh
set -e
# Automatically added by dh_installinit/13.20
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/g15daemon" ] ; then
	invoke-rc.d --skip-systemd-native g15daemon stop || exit 1
fi
# End automatically added section
