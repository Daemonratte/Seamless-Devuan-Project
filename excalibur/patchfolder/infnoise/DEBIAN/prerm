#!/bin/sh
set -e
# Automatically added by dh_installsystemd/13.11.9
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -d /run/systemd/system ] ; then
	deb-systemd-invoke stop 'infnoise.service' >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_installinit/13.11.9
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/infnoise" ] ; then
	invoke-rc.d --skip-systemd-native infnoise stop || true
fi
# End automatically added section
