#!/bin/sh
set -e
# Automatically added by dh_installinit/13.20
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/globus-scheduler-event-generator" ] ; then
	invoke-rc.d --skip-systemd-native globus-scheduler-event-generator stop || exit 1
fi
# End automatically added section
