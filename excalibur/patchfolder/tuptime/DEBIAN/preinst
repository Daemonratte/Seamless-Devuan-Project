#!/bin/sh
set -e
# Automatically added by dh_installinit/13.18
if [ "$1" = "install" ] && [ -n "$2" ] && [ -e "/etc/init.d/tuptime" ] ; then
	chmod +x "/etc/init.d/tuptime" >/dev/null || true
fi
# End automatically added section
