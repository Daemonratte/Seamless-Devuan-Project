#!/bin/sh
set -e
# Automatically added by dh_installinit/13.24.2
if [ "$1" = "remove" ] && [ -x "/etc/init.d/pdns-recursor" ] ; then
	chmod -x "/etc/init.d/pdns-recursor" >/dev/null || true
fi
if [ -z "$DPKG_ROOT" ] && [ "$1" = "purge" ] ; then
	update-rc.d pdns-recursor remove >/dev/null
fi
# End automatically added section
