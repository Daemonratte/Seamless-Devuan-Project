#! /bin/sh

set -e

case "$1" in
       purge)

		rm -f \
			/etc/nullmailer/defaultdomain \
			/etc/nullmailer/defaultdomain.dpkg-* \
			/etc/nullmailer/remotes \
			/etc/nullmailer/remotes.dpkg-* \
			/etc/nullmailer/adminaddr \
			/etc/nullmailer/adminaddr.dpkg-* \
			/etc/nullmailer/defaulthost \
			/etc/nullmailer/defaulthost.dpkg-*

        ;;

esac

# Automatically added by dh_installinit/13.23
if [ "$1" = "remove" ] && [ -x "/etc/init.d/nullmailer" ] ; then
	chmod -x "/etc/init.d/nullmailer" >/dev/null || true
fi
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = "purge" ] ; then
	update-rc.d nullmailer remove >/dev/null
fi
# End automatically added section
# Automatically added by dh_systemd_enable/13.23
if [ "$1" = "purge" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper purge 'nullmailer.service' >/dev/null || true
	fi
fi
# End automatically added section
# Automatically added by dh_installdebconf/13.23
if [ "$1" = purge ] && [ -e /usr/share/debconf/confmodule ]; then
	. /usr/share/debconf/confmodule
	db_purge
fi
# End automatically added section


exit 0
