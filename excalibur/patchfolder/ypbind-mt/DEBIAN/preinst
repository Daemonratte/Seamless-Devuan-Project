#!/bin/sh
set -e
# Automatically added by dh_installinit/13.15.2
if [ "$1" = "install" ] && [ -n "$2" ] && [ -e "/etc/init.d/ypbind" ] ; then
	chmod +x "/etc/init.d/ypbind" >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_installsystemd/13.15.2
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = upgrade ] && [ -d /run/systemd/system ] ; then
	deb-systemd-invoke stop 'ypbind.service' >/dev/null || true
fi
# End automatically added section
