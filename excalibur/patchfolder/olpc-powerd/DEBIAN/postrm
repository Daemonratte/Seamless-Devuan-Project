#!/bin/sh
set -e
# Automatically added by dh_installinit/13.15.3
if [ "$1" = "remove" ] && [ -x "/etc/init.d/olpc-powerd" ] ; then
	chmod -x "/etc/init.d/olpc-powerd" >/dev/null || true
fi
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = "purge" ] ; then
	update-rc.d olpc-powerd remove >/dev/null
fi
# End automatically added section
