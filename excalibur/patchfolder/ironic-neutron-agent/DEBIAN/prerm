#!/bin/sh
set -e
# Automatically added by dh_installinit/13.24.1
if [ -z "$DPKG_ROOT" ] && [ "$1" = remove ] && [ -x "/etc/init.d/ironic-neutron-agent" ] ; then
	invoke-rc.d ironic-neutron-agent stop || true
fi
# End automatically added section
