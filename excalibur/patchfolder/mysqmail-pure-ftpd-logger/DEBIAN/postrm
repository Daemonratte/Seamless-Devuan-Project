#!/bin/sh
set -e
# Automatically added by dh_installinit/13.24.1
if [ "$1" = "remove" ] && [ -x "/etc/init.d/mysqmail-pure-ftpd-logger" ] ; then
	chmod -x "/etc/init.d/mysqmail-pure-ftpd-logger" >/dev/null || true
fi
if [ -z "$DPKG_ROOT" ] && [ "$1" = "purge" ] ; then
	update-rc.d mysqmail-pure-ftpd-logger remove >/dev/null
fi
# End automatically added section
