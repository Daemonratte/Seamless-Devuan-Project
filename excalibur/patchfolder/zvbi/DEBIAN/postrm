#!/bin/sh
set -e
# Automatically added by dh_installinit/13.24.1
if [ "$1" = "remove" ] && [ -x "/etc/init.d/zvbi" ] ; then
	chmod -x "/etc/init.d/zvbi" >/dev/null || true
fi
if [ -z "$DPKG_ROOT" ] && [ "$1" = "purge" ] ; then
	update-rc.d zvbi remove >/dev/null
fi
# End automatically added section
