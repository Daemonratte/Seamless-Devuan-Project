#!/bin/sh
set -e
# Automatically added by dh_installinit/13.24.2
if [ -z "$DPKG_ROOT" ] && [ "$1" = remove ] && [ -x "/etc/init.d/neutron-bgp-dragent" ] ; then
	invoke-rc.d neutron-bgp-dragent stop || true
fi
# End automatically added section
