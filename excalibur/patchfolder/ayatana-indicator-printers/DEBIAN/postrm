#!/bin/sh
set -e
# Automatically added by dh_installsystemduser/13.15.3
if [ "$1" = "purge" ]; then
	if [ -z "${DPKG_ROOT:-}" ] && [ -x "/usr/bin/deb-systemd-helper" ] ; then
		deb-systemd-helper --user purge 'ayatana-indicator-printers.service' >/dev/null || true
	fi
fi
# End automatically added section
