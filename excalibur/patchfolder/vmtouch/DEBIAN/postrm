#!/bin/sh
set -e
# Automatically added by dh_installinit/13.24.1
if [ "$1" = "remove" ] && [ -x "/etc/init.d/vmtouch" ] ; then
	chmod -x "/etc/init.d/vmtouch" >/dev/null || true
fi
if [ -z "$DPKG_ROOT" ] && [ "$1" = "purge" ] ; then
	update-rc.d vmtouch remove >/dev/null
fi
# End automatically added section
