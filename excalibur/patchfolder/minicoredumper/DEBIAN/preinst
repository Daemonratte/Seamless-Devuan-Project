#!/bin/sh
set -e
# Automatically added by dh_installinit/13.20
if [ "$1" = "install" ] && [ -n "$2" ] && [ -e "/etc/init.d/minicoredumper" ] ; then
	chmod +x "/etc/init.d/minicoredumper" >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_installsystemd/13.20
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = upgrade ] && [ -d /run/systemd/system ] ; then
	deb-systemd-invoke stop 'minicoredumper_regd.service' >/dev/null || true
fi
# End automatically added section
