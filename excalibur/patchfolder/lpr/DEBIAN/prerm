#!/bin/sh
set -e
# Automatically added by dh_installinit/13.23
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/lpd" ] ; then
	invoke-rc.d lpd stop || exit 1
fi
# End automatically added section
