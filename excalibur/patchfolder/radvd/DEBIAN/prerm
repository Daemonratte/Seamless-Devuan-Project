#!/bin/sh
set -e
# Automatically added by dh_installinit/13.24.1
if [ -z "$DPKG_ROOT" ] && [ "$1" = remove ] && [ -x "/etc/init.d/radvd" ] ; then
	invoke-rc.d radvd stop || true
fi
# End automatically added section
