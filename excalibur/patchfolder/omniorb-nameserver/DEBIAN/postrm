#! /bin/sh
# postrm script for omniorb-nameserver

set -e

LIBDIR=/var/lib/omniorb/
LOGFILE=/var/log/omniorb-nameserver.log

case "$1" in
    purge)
        rm -rf $LIBDIR  || true
        rm -f $LOGFILE || true
        ;;
    abort-install|remove|upgrade|abort-upgrade|failed-upgrade|disappear)
        true
        ;;
    *)
        echo "postrm called with unknown argument \`$1'" >&2
        exit 1

esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

# Automatically added by dh_installsystemd/13.24.2
if [ "$1" = remove ] && [ -d /run/systemd/system ] ; then
	systemctl --system daemon-reload >/dev/null || true
fi
# End automatically added section
# Automatically added by dh_installsystemd/13.24.2
if [ "$1" = "purge" ]; then
	if [ -x "/usr/bin/deb-systemd-helper" ]; then
		deb-systemd-helper purge 'omniorb-nameserver.service' >/dev/null || true
	fi
fi
# End automatically added section


exit 0
