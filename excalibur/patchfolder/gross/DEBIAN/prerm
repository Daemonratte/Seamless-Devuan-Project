#!/bin/sh
set -e
# Automatically added by dh_installinit/13.15.3
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/gross" ] ; then
	invoke-rc.d gross stop || exit 1
fi
# End automatically added section
