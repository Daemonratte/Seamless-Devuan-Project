#!/bin/sh
set -e
# Automatically added by dh_installinit/13.20
if [ "$1" = "install" ] && [ -n "$2" ] && [ -e "/etc/init.d/net-acct" ] ; then
	chmod +x "/etc/init.d/net-acct" >/dev/null || true
fi
# End automatically added section
