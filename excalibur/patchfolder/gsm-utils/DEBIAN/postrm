#!/bin/sh
set -e
# Automatically added by dh_installinit/13.20
if [ "$1" = "remove" ] && [ -x "/etc/init.d/gsm-utils" ] ; then
	chmod -x "/etc/init.d/gsm-utils" >/dev/null || true
fi
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = "purge" ] ; then
	update-rc.d gsm-utils remove >/dev/null
fi
# End automatically added section
